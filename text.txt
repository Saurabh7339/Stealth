FROM python:3.10-slim  # or any other Debian/Ubuntu-based base image

# Update and install the latest freetype
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
    libfreetype6 \
    libfreetype6-dev && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*
